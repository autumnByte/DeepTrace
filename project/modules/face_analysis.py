def analyze_faces(frames):
    """
    Input:
        frames (list): list of frame file paths
    Output:
        face_scores (dict): {frame_path: face_anomaly_score}
    """
    face_scores = {}
    return face_scores
